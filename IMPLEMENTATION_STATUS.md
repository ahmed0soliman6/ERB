# حالة تنفيذ تصميم ERB

## ما تم تنفيذه

تم إنشاء مشروع WinForms مستهدف لـ .NET Framework 4.8 وبنية x86. تمت إضافة اتصال SQLite محلي وملف Schema شامل للمخازن والأصناف والوحدات والمستندات والحركات والجرد والمستخدمين والأدوار وسجل التدقيق والترخيص والنسخ الاحتياطي.

تمت إضافة قاعدة `Stock Movements = Single Source of Truth` داخل مخطط البيانات. خدمة الحركة تتحقق من الرصيد داخل Transaction، وتمنع الرصيد السالب افتراضيًا. التحويل ينشئ خروجًا من المصدر ودخولًا إلى الوجهة داخل المعاملة نفسها.

تمت إضافة Dashboard أولية تقرأ الرصيد من مجموع `signed_quantity` في `stock_movements`، ولا تعتمد على عمود رصيد قابل للتعديل.

تمت إضافة تبويبات التوريد والتحويل والصرف والاستهلاك. يستطيع المستخدم اختيار المخزن والصنف والكمية وإضافة عدة أسطر ثم اعتماد المستند. عند الاعتماد تنفذ الخدمة معاملة SQLite واحدة تنشئ رأس المستند وأسطره وحركاته، ويولد التحويل حركة خروج وحركة دخول ذريتين. تمنع خدمة الحركة الصرف والاستهلاك والتحويل من تجاوز الرصيد المتاح.

## ما لم يكتمل بعد

1. شاشة التفعيل والتحقق من التوقيع الرقمي للترخيص.
2. Login حقيقي وتطبيق RBAC على الشاشات والمخازن.
3. شاشات CRUD للمخازن والأصناف والوحدات والموردين.
4. حفظ المسودات وإعادة فتحها، مع استكمال دورة Draft/Approve/Void للمستندات الحالية.
5. شاشة الجرد واعتماد COUNT_ADJUSTMENT.
6. Excel Import/Mapping ومراجعة المشاكل قبل الترحيل.
7. Backup/Restore والتحقق من Hash وسلامة SQLite.
8. تقارير وتصدير Excel/PDF.
9. Installer Offline واختبار الأجهزة الفعلية.
10. اختبارات وحدة وتكامل على Windows 7 SP1 وWindows 8.1 وWindows 10 وWindows 11.

## معيار عدم الإطلاق

لا يطلق المشروع كنسخة إنتاجية قبل إكمال البنود السابقة، وإجراء اختبار قبول موثق، واختبار الاستعادة من Backup، واختبار انتهاء الترخيص وتغيير تاريخ Windows دون فقد البيانات.
