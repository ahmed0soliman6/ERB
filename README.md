# SoliMedical-ERB — نظام إدارة مخازن المجمع الطبي

هذا المستودع يحتوي على نواة تطبيق Desktop محلي وفق التصميم المعتمد لنظام إدارة المخازن.

## القرار التقني

- C# وWindows Forms.
- .NET Framework 4.8.
- هدف البناء `x86` لدعم Windows 7 SP1 32-bit و64-bit.
- SQLite محلية داخل ملف بيانات واحد.
- لا توجد Firebase أو Supabase أو Cloud Database أو API أو Internet dependency.
- قاعدة الحقيقة الكمية هي `stock_movements`.

## الموجود في هذه النسخة

- Solution ومشروع WinForms قابل للبناء على Windows مع Visual Studio.
- إنشاء قاعدة SQLite محلية تلقائيًا عند أول تشغيل.
- مخطط أولي كامل للجداول الرئيسية والحركات والمستندات والجرد والصلاحيات والتدقيق والترخيص والنسخ الاحتياطي.
- خدمة حركات تمنع الرصيد السالب وتنفذ التحويل عبر معاملة ذرية.
- واجهة Dashboard أولية تعرض الأرصدة الناتجة من الحركات.
- شاشات تشغيلية أولية للتوريد والتحويل والصرف والاستهلاك.
- إضافة أسطر متعددة لكل مستند ثم اعتماد المستند داخل معاملة SQLite واحدة.
- التحويل ينشئ خروجًا من المصدر ودخولًا إلى الوجهة ذريًا.
- الصرف والاستهلاك يتحققان من الرصيد قبل إنشاء الحركة.
- Login محلي آمن باستخدام PBKDF2، وإنشاء المسؤول الأول عند أول تشغيل.
- RBAC مبني على الأدوار ADMIN وSTORE_MANAGER وSTORE_USER وVIEWER.
- حفظ مسودات التوريد والتحويل والصرف والاستهلاك وإعادة فتحها واعتمادها.
- شاشة إدارة المستخدمين للمسؤول مع إنشاء المستخدم وتعيين الدور وتفعيل/تعطيل الحساب.
- تخصيص مخازن المستخدمين عبر `user_warehouses` مع فرض القيد في الواجهة وخدمة المستندات.
- إدارة تفصيلية لصلاحيات كل دور عبر `role_permissions`.
- تضمين شعار Soli Medical بصيغتي SVG وPNG داخل مخرجات التطبيق.
- لوحة تقارير تحليلية للفترة والمخزن: ملخص الحركات، الأعلى صرفًا، وتحت الحد الأدنى.
- نسخ SQLite تلقائية عند بدء التشغيل، ونسخ يدوية، والتحقق من SHA-256 قبل الاستعادة.
- استعادة مؤجلة آمنة عند إعادة التشغيل مع نسخة حماية قبل الاستبدال.
- نظام تنبيهات فوري للأصناف عند بلوغ حد الطلب أو الحد الأدنى، مع شريط تنبيه ولوحة تفاصيل.
- واجهة محدثة باسم SoliMedical-ERB مع شريط حالة واضح وأزرار إجراءات مباشرة.
- مخازن افتراضية: الرئيسي، الطوارئ، أدوية العمليات، ومستلزمات العمليات.
- وحدات قياس أولية منفصلة عن الملاحظات.

## البناء على Windows

1. ثبت Visual Studio مع Desktop development for .NET Framework.
2. افتح `ERB.sln`.
3. استعد حزمة `System.Data.SQLite.Core` من NuGet أثناء التطوير فقط.
4. اختر `Release | x86`.
5. نفذ Build.
6. انسخ مجلد `Database` بجانب ملف التشغيل في حزمة التوزيع.

التطبيق النهائي لا يحتاج إلى Node.js أو Python. أما استعادة حزمة NuGet فهي خطوة بناء للمطور وليست اعتماد تشغيل للمستخدم النهائي.

## البيانات

يضع التطبيق قاعدة البيانات في:

```text
%LOCALAPPDATA%\ERB\inventory.db
```

ولا يضعها داخل `Program Files` حتى لا يحتاج المستخدم إلى صلاحيات Administrator أثناء التشغيل.

## الحالة الحالية

هذه نسخة تجريبية قابلة للبناء وليست إصدارًا إنتاجيًا كاملًا بعد. يجب اختبار حزمة التشغيل على Windows 7 SP1 وWindows 8.1 وWindows 10 وWindows 11 قبل التسليم الإنتاجي.

لا يتم اعتبار المشروع مكتملًا تجاريًا قبل اعتماد اختبارات Windows 7 SP1 32-bit و64-bit وWindows 8.1 وWindows 10 وWindows 11.
